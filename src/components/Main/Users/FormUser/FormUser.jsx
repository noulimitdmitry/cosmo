import styles from "./FormUser.module.css";

const FormUser = (props) => {
  return (
    <div className={styles.block}>
      <button className={styles.button}>SET USER</button>
    </div>
  );
};

export default FormUser;
